{
  "name": "smart-city-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend run start",
    "init:db": "npm --prefix backend run init:db",
    "seed": "npm --prefix backend run seed"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}
